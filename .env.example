# MalgeunTube 환경 변수 설정 예시
# 이 파일을 복사하여 .env 파일을 생성하고 값을 설정하세요.

# Flask 환경 설정
# development, production, testing 중 선택
FLASK_ENV=development

# 시크릿 키 (프로덕션 환경에서 반드시 설정)
# python -c "import os; print(os.urandom(24).hex())" 명령으로 생성 가능
SECRET_KEY=your-secret-key-here

# 데이터베이스 URL (기본값: SQLite 사용)
# DATABASE_URL=sqlite:///data/malgeuntube.db

# 로그 레벨 (DEBUG, INFO, WARNING, ERROR, CRITICAL)
LOG_LEVEL=INFO

# Redis URL (캐시용, 선택사항)
# REDIS_URL=redis://localhost:6379/0
